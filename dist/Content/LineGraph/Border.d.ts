import * as React from "react";
declare type BorderProps = {
    graphHeight: number;
    graphWidth: number;
    color: string;
    width: number;
};
declare const Border: React.FC<BorderProps>;
export { Border };
